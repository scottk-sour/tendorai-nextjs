// TODO: Import this in components instead of hardcoding.
// Currently hardcoded as "3 of 50" in 17 files.
// Update EARLY_ADOPTER_COUNT here and manually update
// the string "3 of 50" in each file when count changes.
export const EARLY_ADOPTER_TOTAL = 50;
export const EARLY_ADOPTER_COUNT = 3;
export const EARLY_ADOPTER_REMAINING = EARLY_ADOPTER_TOTAL - EARLY_ADOPTER_COUNT;
